[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Unit tests that mock all dependencies
    integration: Integration tests that test component interactions
    email: Tests that involve email functionality
    marcaje: Tests for marcaje functionality
